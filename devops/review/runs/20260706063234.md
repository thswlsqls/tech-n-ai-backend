# 실행 보고 2026-07-06 06:32 (첫 실행)

- 대상: devops/ 전체
- 도구: Terraform v1.15.6, TFLint 0.63.1 사용 가능 / AWS 자격증명 없음 → plan 검증 불가, plan diff 를 만드는 변경은 전부 보고로 돌림
- 검증한 주장: terraform 트리(AWS 스펙·결함·환경 차이) 약 35건, 문서↔코드 대조 약 15건, 앱 코드 대조 약 20건

## 고친 것 (7건, 전부 plan 무영향 확인)
1. results/09-iac-terraform.md — 버전 핀 예시 `~> 1.9.5`/`~> 5.60` → `~> 1.15.0`/`~> 5.100` (본문 §1.3, §7.1), backend 잠금 주석의 "1.9라서 use_lockfile 미지원" 전제 폐기
2. results/07c-cicd-infra.md — CI 예시 `terraform_version: 1.9.5` → `1.15.6` (2곳)
3. results/08-observability.md — 대시보드 인용 `main.tf:114` → `:123` (ok_actions 추가로 라인 밀림)
4. modules/cloudfront-spa/variables.tf — 죽은 노브 `default_ttl_seconds` 제거 (관리형 정책 CachingOptimized 사용이라 무효과, 호출부 없음)
5. envs/{dev,beta,prod}/services.tf — 미사용 `local.all_workload_sg_ids` 제거 + "모든 워크로드가 접근" 낡은 주석을 실제(서비스별 최소 권한)로 수정, 3환경 동일 적용
6. modules/msk-provisioned/outputs.tf — JMX 출력이 SASL 브로커 목록을 반환하던 복붙 오류를 브로커호스트:11001 리스트로 수정 (소비처 없음 확인)
7. devops/reminder/terraform-validation-followups.md — 해소 항목 현행화 (#1 코드 완료·인증서만 남음, lock 파일 커밋 완료, 미사용 선언 2건 제거)

재검증: fmt OK / envs 3개+bootstrap validate 통과 / envs 복사본 동기화 유지 / tflint 잔여 경고는 의도된 `permissions_boundary_managed` 1건뿐

## 보고로 남긴 것
knowledge/terraform.md·app-vs-devops.md 의 "미해결" 절 참고. 요지: dev apply 를 깨뜨릴 수 있는 빈 IAM statement(task_roles), prod apply 를 깨뜨릴 수 있는 monitoring_role_arn 부재·인증서 자리표시, autoscaling 인자 누락, 앱 yml 의 평문 DB 계정·k8s 식 라우팅 uri 등.

## 메모리에 새로 적은 것
- knowledge/aws-facts.md — CachingOptimized ID·MSK JMX 포트 (근거 URL 포함)
- knowledge/terraform.md — 버전 핀·복사본 설계·매트릭스 일치 등 검증 완료 5건, 거짓 양성 4건, 미해결 11건
- knowledge/app-vs-devops.md — 포트·consumer 매핑 일치 3건, 코드↔인프라 불일치 6건
