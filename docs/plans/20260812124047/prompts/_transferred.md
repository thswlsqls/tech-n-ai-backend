# 이관 대장 — 이 문서로는 해결할 수 없는 항목

다른 문서를 고쳐야 하거나 저장소 밖 사실이라, `02-eval-observability.md`를 아무리 고쳐도
없어지지 않는 것들이다. 기준 8항에 따라 **감점하지 않는다.** 위원이 새로 발견하면 이 대장에
추가만 한다.

네 라운드 동안 아래 항목 대부분이 매번 다시 지적됐다. 그래서 대장으로 뺐다.

- 마지막 갱신: 라운드 5 종료 시점 (2026-08-15)
- **라운드 5에서 새로 추가된 항목은 없다.** 위원 다섯이 기존 항목을 재확인만 했고
  감점으로 올린 건은 0건이다. 대장으로 빼는 처리가 의도대로 작동했다

## 다른 계획 문서를 고쳐야 하는 것

| 항목 | 내용 | 최초 지적 | 재등장 |
|------|------|-----------|--------|
| `05-backlog.md`의 비용 주인 서술 | `:62`, `:162`, `:187`이 아직 02를 비용 측정의 주인으로 적는다. 02는 라운드 3에서 오프라인 집계만 맡기로 정리했다 | R2 | R3, R4 |
| `README.md:72`의 선후 서술 | "01은 나머지 전부의 선행 조건이다"라고 못박는데, 02에서 01이 필요한 것은 (e)에서 `langchain4j-observation`을 고를 때뿐이다. **02가 맞고 README가 틀렸다** | R1 | R2, R4 |
| `01-langchain4j-upgrade.md:16`의 사실 오류 | "`langchain4j-observation`, `langchain4j-guardrails`도 최신 라인에만 있다"고 적었으나 `1.12.1-beta21`부터 있고 01이 검토 중인 1.13.x에도 있다. **02가 맞고 01이 틀렸다** | R4 | — |
| `03-graphrag.md`가 02의 요구를 안 받음 | 02가 "03·04는 기존 검색 경로를 설정으로 끌 수 있게 남겨 둘 것"을 요구하는데(`02:468-469` 무렵), 03은 02를 네 곳에서 참조하면서도 이 항목이 없다 | R4 | — |

**처리 방법**: 02 평가가 끝난 뒤 별도 작업으로 네 문서를 한 번에 맞춘다. 02 안에는 이미
"그쪽을 함께 고쳐야 한다. 이 문서 범위 밖이라 남겨 둔다"고 적혀 있으므로 추가 서술은 필요 없다.

## 저장소에서 확인할 수 없는 것

| 항목 | 왜 확인 못 하나 | 최초 지적 | 재등장 |
|------|-----------------|-----------|--------|
| Atlas 벡터 인덱스에 `update_type`·`published_at`이 filter로 등록됐는지 | Atlas 콘솔 쪽 사실이다. 저장소의 인덱스 생성 JSON은 있으나 실제 인덱스와 같다는 보장이 없다 | R1 | R2, R3, R4 |
| 임계값 0.7이 실제로 정답을 얼마나 자르는지 | Atlas 접속과 실제 데이터가 필요하다 | R3 | R4 |
| 한국어 질문 대 영어 문서 임베딩 성능 | 위와 같다 | R3 | R4 |
| "근거 없음" 오검출률이 항상 100% 근처에 붙지 않는지 | 위와 같다. cosine 0.4가 낮아 어떤 질문에도 문서가 걸릴 수 있다 | R4 | — |

**처리 방법**: 넷 다 문서의 "제약·리스크" 절에 행동 항목으로 이미 들어가 있다
("골든셋을 만들기 전에 Atlas 콘솔에서 확인한다"). 구현 첫 단계에서 함께 확인한다.
**미확인이라는 사실 자체는 감점 사유가 아니다** — 기준 3항.
